---
const links = [
	{ href: '/', label: 'Inicio' },
	{ href: '/#servicios', label: 'Servicios' },
	{ href: '/#nosotros', label: 'Nosotros' },
	{ href: '/#contacto', label: 'Contacto' },
];
---

<header class="sticky top-0 z-50 border-b border-zinc-200 bg-white/90 backdrop-blur">
	<div class="mx-auto flex h-16 max-w-6xl items-center justify-between px-4">
		<a href="/" class="text-lg font-bold tracking-tight">
			Autotalleres <span class="text-brand">Carmovil</span>
		</a>

		<nav class="hidden items-center gap-8 md:flex">
			{links.map((link) => (
				<a href={link.href} class="text-sm font-medium text-zinc-600 transition hover:text-brand">
					{link.label}
				</a>
			))}
			<a
				href="/#contacto"
				class="rounded-md bg-brand px-4 py-2 text-sm font-semibold text-white transition hover:bg-brand-dark"
			>
				Pedir cita
			</a>
		</nav>

		<button
			id="menu-toggle"
			type="button"
			class="rounded-md p-2 text-zinc-700 md:hidden"
			aria-label="Abrir menú"
			aria-controls="mobile-menu"
			aria-expanded="false"
		>
			<svg class="h-6 w-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
				<path stroke-linecap="round" d="M4 6h16M4 12h16M4 18h16" />
			</svg>
		</button>
	</div>

	<nav id="mobile-menu" class="hidden border-t border-zinc-200 px-4 py-4 md:hidden">
		<div class="flex flex-col gap-3">
			{links.map((link) => (
				<a href={link.href} class="font-medium text-zinc-700 hover:text-brand">{link.label}</a>
			))}
			<a
				href="/#contacto"
				class="mt-2 rounded-md bg-brand px-4 py-2 text-center font-semibold text-white hover:bg-brand-dark"
			>
				Pedir cita
			</a>
		</div>
	</nav>
</header>

<script>
	const toggle = document.getElementById('menu-toggle');
	const menu = document.getElementById('mobile-menu');

	toggle?.addEventListener('click', () => {
		const open = menu?.classList.toggle('hidden') === false;
		toggle.setAttribute('aria-expanded', String(open));
	});

	menu?.querySelectorAll('a').forEach((a) =>
		a.addEventListener('click', () => {
			menu.classList.add('hidden');
			toggle?.setAttribute('aria-expanded', 'false');
		}),
	);
</script>
